apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 用 kustomize 把下面这些 YAML 当成一个整体部署：
# kubectl apply -k deploy/k8s
resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
